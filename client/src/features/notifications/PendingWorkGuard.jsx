// This component now intentionally stays non-blocking.
// Overdue reminders are surfaced by ReminderToastNotifier, but users are not redirected.

export default function PendingWorkGuard() {
  return null;
}
